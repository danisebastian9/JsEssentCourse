"use strict"

// Trabajar fechas

var fecha = new Date();  // Esta variable tiene la fecha exacta completa del momento en que se ejecuta

fecha.getDay() // El dia de la semana en que nos encontramos. Martes(2)
fecha.getDate() // El dia del mes en que nos encontramos Jun 24 (24)
fecha.getHours() // Cual es la hora en que se ejecuta la instruccion
fecha.setDate(5) // devuelve un valor numero convertido en fecha. En este caso cambia la fecha al dia 5 del mes y año actual. 


